<UserControl x:Class="ICSharpCode.AiAgent.AiAgentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ICSharpCode.AiAgent"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="AI Agent" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="StatusText" FontSize="10" Foreground="Gray" VerticalAlignment="Center" Margin="8 0 0 0"/>
            <Button Grid.Column="2" x:Name="SettingsButton" Content="Settings" Width="70" Height="22" FontSize="11"/>
        </Grid>
        
        <Grid Grid.Row="1" Margin="5">
            <local:ThinkingStreamControl x:Name="StreamOutput"/>
        </Grid>
        
        <Grid Grid.Row="2" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Action:" VerticalAlignment="Center" Margin="0 0 4 0"/>
            <ComboBox Grid.Column="1" x:Name="ActionCombo" Height="28" VerticalAlignment="Center">
                <ComboBoxItem Tag="generate" IsSelected="True">Code Generation</ComboBoxItem>
                <ComboBoxItem Tag="explain">Explain Code</ComboBoxItem>
                <ComboBoxItem Tag="optimize">Optimize Code</ComboBoxItem>
                <ComboBoxItem Tag="refactor">Refactor Code</ComboBoxItem>
                <ComboBoxItem Tag="debug">Debug Code</ComboBoxItem>
            </ComboBox>
            <Button Grid.Column="3" x:Name="BtnExecute" Content="Execute" IsEnabled="False" Width="80" Height="28" Margin="8 0 0 0"/>
        </Grid>
        
        <Grid Grid.Row="3" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="Prompt:"/>
            <TextBox Grid.Row="1" x:Name="PromptTextBox" AcceptsReturn="True" Height="70" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
        </Grid>
    </Grid>
</UserControl>