<UserControl x:Class="ICSharpCode.AiAgent.AiAgentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ICSharpCode.AiAgent"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="AI Agent" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="StatusText" FontSize="10" Foreground="Gray" VerticalAlignment="Center" Margin="8 0 0 0"/>
            <Button Grid.Column="2" x:Name="SettingsButton" Content="Settings" Width="70" Height="22" FontSize="11"/>
        </Grid>
        
        <Grid Grid.Row="1" Margin="5">
            <local:ThinkingStreamControl x:Name="StreamOutput"/>
        </Grid>
        
        <Grid Grid.Row="2" Margin="5" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Model:" VerticalAlignment="Center" Margin="0 0 4 0"/>
            <ComboBox Grid.Column="1" x:Name="ModelCombo" Height="26" VerticalAlignment="Center" Margin="0 0 6 0"/>
            <Label Grid.Column="2" Content="Skill:" VerticalAlignment="Center" Margin="0 0 4 0"/>
            <ComboBox Grid.Column="3" x:Name="SkillCombo" Height="26" VerticalAlignment="Center" Margin="0 0 6 0"/>
            <Label Grid.Column="4" Content="Action:" VerticalAlignment="Center" Margin="0 0 4 0"/>
            <ComboBox Grid.Column="5" x:Name="ActionCombo" Height="26" VerticalAlignment="Center">
                <ComboBoxItem Tag="execute" IsSelected="True">Execute Prompt</ComboBoxItem>
                <ComboBoxItem Tag="chat">Free Chat</ComboBoxItem>
            </ComboBox>
            <Button Grid.Column="6" x:Name="BtnExecute" Content="Execute" IsEnabled="False" Width="80" Height="26" Margin="6 0 0 0"/>
        </Grid>
        
        <Grid Grid.Row="3" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="Prompt:"/>
            <TextBox Grid.Row="1" x:Name="PromptTextBox" AcceptsReturn="True" Height="70" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
        </Grid>
    </Grid>
</UserControl>