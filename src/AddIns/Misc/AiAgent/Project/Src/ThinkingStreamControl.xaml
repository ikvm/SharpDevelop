<UserControl x:Class="ICSharpCode.AiAgent.ThinkingStreamControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="4 2 4 2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="TitleText" Text="Output" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="ProgressText" Text="" FontSize="10" Foreground="Gray" VerticalAlignment="Center" Margin="6 0 4 0"/>
            <Button Grid.Column="2" x:Name="PreviewToggle" Content="Preview" Width="55" Height="20" FontSize="10" Margin="0 0 4 0" Click="PreviewToggle_Click" IsEnabled="False"/>
            <Button Grid.Column="3" x:Name="CancelButton" Content="Cancel" Width="50" Height="20" FontSize="10" Visibility="Collapsed" Click="CancelButton_Click"/>
        </Grid>
        
        <Border Grid.Row="1" BorderBrush="#CCC" BorderThickness="1" CornerRadius="2" Margin="2">
            <Grid>
                <ScrollViewer x:Name="RawScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <TextBox x:Name="OutputTextBox"
                             IsReadOnly="True"
                             FontFamily="Consolas"
                             FontSize="11"
                             TextWrapping="Wrap"
                             Padding="6"
                             BorderThickness="0"
                             Background="#FAFAFA"
                             MinHeight="60"/>
                </ScrollViewer>
                <WebBrowser x:Name="PreviewBrowser"
                            Visibility="Collapsed"
                            MinHeight="60"/>
            </Grid>
        </Border>
        
        <Grid Grid.Row="2" Margin="4 2 4 2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="StreamStatusText" Text="" FontSize="10" Foreground="Gray" VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="CopyButton" Content="Copy" Width="50" Height="20" FontSize="10" Margin="0 0 4 0" Click="CopyButton_Click"/>
            <Button Grid.Column="2" x:Name="CopyHtmlButton" Content="Copy HTML" Width="60" Height="20" FontSize="10" Margin="0 0 4 0" Click="CopyHtmlButton_Click"/>
            <Button Grid.Column="3" x:Name="InsertButton" Content="Insert" Width="50" Height="20" FontSize="10" Click="InsertButton_Click"/>
        </Grid>
    </Grid>
</UserControl>